# Archived Frontend Scaffold

The React/Vite frontend previously present in this repository has been archived as a reference-only scaffold.

- Active live deployment: the PHP/Tailwind XAMPP application under the root folder.
- Archived scaffold location: `frontend-archive/`.
- The archived folder is not required for the live deployment and may be removed once any needed design references are extracted.

This file documents the transition to a single PHP-based system for deployment under `http://localhost/Smart-Procurement/`.
